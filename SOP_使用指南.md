# iCloud 照片迁移助手 - 使用指南 (SOP)

## 📋 目录

1. [概述](#概述)
2. [系统要求](#系统要求)
3. [准备工作](#准备工作)
4. [安装步骤](#安装步骤)
5. [配置说明](#配置说明)
6. [使用流程](#使用流程)
7. [功能说明](#功能说明)
8. [常见问题](#常见问题)
9. [注意事项](#注意事项)
10. [故障排除](#故障排除)

---

## 概述

**iCloud 照片迁移助手** 是一款用于将 iCloud 照片和视频批量下载到本地存储（NAS、外接硬盘、本地文件夹）的工具。

### 主要功能

- ✅ 批量下载 iCloud 照片和视频
- ✅ 多线程下载（速度提升 3-5 倍）
- ✅ 断点续传（中断后可继续）
- ✅ 自动重试（网络异常自动恢复）
- ✅ 按日期自动组织文件夹
- ✅ 实时进度显示
- ✅ 日志记录

---

## 系统要求

| 项目 | 要求 |
|------|------|
| 操作系统 | macOS 10.15 或更高版本 |
| Python | 3.9 或更高版本 |
| 网络 | 稳定的网络连接 |
| 存储空间 | 至少 100GB 可用空间 |
| iCloud | 已登录的 Apple ID |

---

## 准备工作

### 1. 检查 Python 版本

打开终端，输入：

```bash
python3 --version
```

如果显示 `Python 3.9.x` 或更高版本，则满足要求。

如果未安装 Python，请访问：https://www.python.org/downloads/

### 2. 准备存储设备

选择以下任一方式：

**方式一：NAS（推荐）**
- 确保 NAS 已开机并连接到网络
- 在 Finder 中挂载 NAS：
  - 按 `Cmd + K`
  - 输入：`smb://你的NAS地址`
  - 输入用户名和密码
  - 挂载后会出现在 `/Volumes/` 目录

**方式二：外接硬盘**
- 将外接硬盘连接到 Mac
- 确保硬盘已挂载（出现在桌面或 Finder 侧边栏）

**方式三：本地文件夹**
- 在 Mac 本地创建一个文件夹用于存储照片

### 3. 检查 iCloud 账户

- 确保知道 Apple ID 和密码
- 确保已开启两步验证
- 确保 iCloud 照片已开启

---

## 安装步骤

### 步骤 1：下载程序

将程序文件夹保存到 Mac 上，例如：
```
/Users/你的用户名/Documents/iCloud照片迁移助手/
```

### 步骤 2：安装依赖

打开终端，进入程序目录：

```bash
cd /Users/你的用户名/Documents/iCloud照片迁移助手
```

安装依赖：

```bash
pip3 install -r requirements.txt
```

等待安装完成（约 1-2 分钟）。

### 步骤 3：验证安装

运行以下命令验证：

```bash
python3 -c "import PySide6; print('✅ PySide6 安装成功')"
python3 -c "import yaml; print('✅ PyYAML 安装成功')"
python3 -c "import qrcode; print('✅ qrcode 安装成功')"
```

如果都显示 ✅，则安装成功。

---

## 配置说明

### 配置文件位置

```
/Users/你的用户名/Documents/iCloud照片迁移助手/config.yaml
```

### 配置项说明

| 配置项 | 说明 | 默认值 | 必填 |
|--------|------|--------|------|
| `apple_id` | Apple ID 邮箱 | 空 | 否（可在应用中输入） |
| `nas_path` | 存储路径 | 空 | 是 |
| `cookie_dir` | 认证信息存储目录 | `./cookies` | 否 |
| `folder_format` | 文件夹格式 | `%Y/%Y-%m/%Y-%d` | 否 |
| `threads_num` | 下载线程数 | 3 | 否 |
| `only_new` | 仅下载新文件 | true | 否 |
| `download_photos` | 下载照片 | true | 否 |
| `download_videos` | 下载视频 | true | 否 |
| `skip_live_photos` | 跳过实况照片 | false | 否 |
| `max_retries` | 最大重试次数 | 3 | 否 |

### 配置示例

```yaml
# Apple ID（可选，可在应用中输入）
apple_id: "your@email.com"

# 存储路径（必填）
# NAS 路径示例：
nas_path: "/Volumes/MyNAS/iCloud-Photos"
# 本地路径示例：
# nas_path: "/Users/yourname/Pictures/iCloud-Photos"
# 外接硬盘示例：
# nas_path: "/Volumes/MyExternalDrive/iCloud-Photos"

# 文件夹格式
# %Y = 年, %m = 月, %d = 日
folder_format: "%Y/%Y-%m/%Y-%d"

# 下载线程数（1-10）
threads_num: 3

# 仅下载新文件
only_new: true

# 下载照片
download_photos: true

# 下载视频
download_videos: true

# 最大重试次数
max_retries: 3
```

---

## 使用流程

### 流程图

```
┌─────────────────────────────────────────────────────────────┐
│                        开始                                  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  1. 启动程序                                                 │
│     - 双击「启动iCloud迁移助手.command」                       │
│     - 或终端运行：python3 main.py                            │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  2. 登录 iCloud                                              │
│     - 选择登录方式（扫码/密码）                               │
│     - 输入 Apple ID                                          │
│     - 完成两步验证                                           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  3. 设置存储路径                                             │
│     - 输入 NAS/硬盘/本地路径                                 │
│     - 点击「检测连接」                                       │
│     - 确认路径可用                                           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  4. 开始下载                                                 │
│     - 点击「开始迁移」按钮                                   │
│     - 等待下载完成                                           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  5. 查看结果                                                 │
│     - 查看下载统计                                           │
│     - 查看失败文件列表                                       │
│     - 导出报告（可选）                                       │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        完成                                  │
└─────────────────────────────────────────────────────────────┘
```

### 详细步骤

#### 步骤 1：启动程序

**方式一：双击启动**
1. 打开 Finder
2. 进入 `iCloud照片迁移助手` 文件夹
3. 双击 `启动iCloud迁移助手.command`

**方式二：终端启动**
```bash
cd /Users/你的用户名/Documents/iCloud照片迁移助手
python3 main.py
```

#### 步骤 2：登录 iCloud

1. **选择登录方式**
   - 扫码登录（推荐，更安全）
   - 密码登录

2. **扫码登录流程**
   - 点击「生成二维码」
   - 用 iPhone 扫描二维码
   - 在 iPhone 上确认登录
   - 返回应用点击「我已完成登录」

3. **密码登录流程**
   - 输入 Apple ID
   - 点击「连接 iCloud」
   - 在弹出的窗口中输入密码
   - 输入两步验证码

4. **等待认证成功**
   - 显示「✅ iCloud 认证成功」
   - 显示照片库统计信息

#### 步骤 3：设置存储路径

1. **输入路径**
   - NAS 路径：`/Volumes/你的NAS名称/照片文件夹`
   - 本地路径：`/Users/你的用户名/Pictures/iCloud-Photos`
   - 外接硬盘：`/Volumes/你的硬盘名称/iCloud-Photos`

2. **检测连接**
   - 点击「检测 NAS 连接」按钮
   - 等待显示「✅ NAS 已连接」

3. **确认路径**
   - 确保路径正确
   - 确保有足够存储空间

#### 步骤 4：开始下载

1. **点击「开始迁移」按钮**

2. **监控下载进度**
   - 查看进度条
   - 查看已下载数量
   - 查看下载速度
   - 查看预计剩余时间

3. **控制下载**
   - 暂停：临时停止下载
   - 继续：恢复下载
   - 停止：终止下载（已下载文件保留）

#### 步骤 5：查看结果

1. **查看统计信息**
   - 成功下载数量
   - 跳过数量
   - 失败数量
   - 重复文件数量
   - 总耗时

2. **查看失败文件**
   - 点击「查看失败列表」
   - 记录失败原因
   - 手动重试（可选）

3. **导出报告**
   - 点击「导出报告」
   - 保存为文本文件

---

## 功能说明

### 1. 扫码登录

**优势**
- 更安全（无需输入密码）
- 更便捷（扫码即登录）
- 支持两步验证

**操作流程**
1. 选择「扫码登录」
2. 点击「生成二维码」
3. 用 iPhone 相机扫描
4. 在 iPhone 上确认
5. 返回应用点击「我已完成登录」

### 2. 多线程下载

**说明**
- 默认 3 个并发线程
- 可在配置文件中调整（1-10）
- 线程数越多，速度越快

**配置方法**
编辑 `config.yaml`：
```yaml
threads_num: 5  # 设置为 5 个线程
```

### 3. 断点续传

**说明**
- 下载过程中如果中断（网络断开、程序关闭等）
- 重新启动后会自动继续下载
- 跳过已下载的文件

**使用方法**
- 正常启动程序
- 如果检测到未完成的迁移，会提示是否继续
- 选择「是」继续下载

### 4. 自动重试

**说明**
- 网络异常时自动重试
- 最多重试 3 次（可配置）
- 指数退避延迟（2s, 4s, 8s...）

**配置方法**
编辑 `config.yaml`：
```yaml
max_retries: 5  # 设置为 5 次重试
```

### 5. 文件夹组织

**默认格式**
```
iCloud-Photos/
├── 2024/
│   ├── 2024-01/
│   │   ├── 2024-01-01/
│   │   │   ├── IMG_0001.HEIC
│   │   │   ├── IMG_0002.JPG
│   │   │   └── VID_0001.MOV
│   │   ├── 2024-01-02/
│   │   │   └── ...
│   │   └── ...
│   ├── 2024-02/
│   │   └── ...
│   └── ...
├── 2025/
│   └── ...
└── ...
```

**自定义格式**
编辑 `config.yaml`：
```yaml
# 按年/月组织
folder_format: "%Y/%m"

# 按年/月/日组织
folder_format: "%Y/%Y-%m/%Y-%m-%d"

# 按年-月组织
folder_format: "%Y-%m"
```

### 6. 日志系统

**日志位置**
```
/Users/你的用户名/Documents/iCloud照片迁移助手/logs/
```

**日志文件**
- `app_20250101.log` - 按日期命名
- 自动轮转（每个文件最大 10MB）
- 保留最近 5 个日志文件

**查看日志**
```bash
# 查看最新日志
tail -f logs/app_*.log

# 查看所有日志
cat logs/app_*.log
```

---

## 常见问题

### Q1: 提示「icloudpd 未安装」

**原因**：缺少 icloudpd 依赖

**解决方法**：
```bash
pip3 install icloudpd
```

### Q2: 提示「认证失败」

**原因**：Apple ID 或密码错误

**解决方法**：
1. 确认 Apple ID 正确
2. 确认密码正确
3. 确认已开启两步验证
4. 尝试使用扫码登录

### Q3: 提示「网络连接失败」

**原因**：网络不稳定或被阻止

**解决方法**：
1. 检查网络连接
2. 尝试使用其他网络
3. 关闭 VPN（如果有）
4. 检查防火墙设置

### Q4: 提示「磁盘空间不足」

**原因**：存储空间不够

**解决方法**：
1. 清理存储空间
2. 更换更大的存储设备
3. 使用「仅下载新文件」选项

### Q5: 下载速度很慢

**原因**：网络带宽限制或线程数太少

**解决方法**：
1. 增加线程数：
   ```yaml
   threads_num: 5
   ```
2. 使用更快的网络
3. 避免同时进行其他大流量操作

### Q6: 下载中断了怎么办？

**解决方法**：
1. 重新启动程序
2. 如果提示「发现未完成的迁移」，选择「是」继续
3. 程序会自动跳过已下载的文件

### Q7: 如何查看下载失败的文件？

**解决方法**：
1. 在报告页面点击「查看失败列表」
2. 记录失败原因
3. 手动重试或等待下次自动重试

### Q8: 如何修改配置？

**解决方法**：
1. 打开 `config.yaml` 文件
2. 修改相应的配置项
3. 保存文件
4. 重新启动程序

---

## 注意事项

### 1. 网络要求

- ✅ 需要稳定的网络连接
- ✅ 建议使用有线网络或 5GHz WiFi
- ❌ 避免使用不稳定的公共 WiFi

### 2. 存储空间

- ✅ 确保有足够的存储空间
- ✅ 建议预留 20% 的额外空间
- ❌ 避免存储空间不足导致下载失败

### 3. 电源管理

- ✅ 下载过程中保持 Mac 开机
- ✅ 建议连接电源适配器
- ❌ 避免合盖导致休眠

### 4. 网络安全

- ✅ 使用扫码登录更安全
- ✅ 定期更新密码
- ❌ 避免在公共电脑上使用

### 5. 数据备份

- ✅ 下载完成后建议备份
- ✅ 可以使用 Time Machine 备份
- ❌ 不要删除 iCloud 中的原始文件

---

## 故障排除

### 问题 1：程序无法启动

**检查步骤**：
1. 检查 Python 版本：
   ```bash
   python3 --version
   ```

2. 检查依赖安装：
   ```bash
   pip3 list | grep -E "PySide6|pyyaml|qrcode"
   ```

3. 重新安装依赖：
   ```bash
   pip3 install -r requirements.txt
   ```

### 问题 2：认证失败

**检查步骤**：
1. 确认 Apple ID 正确
2. 确认密码正确
3. 确认已开启两步验证
4. 尝试在 iPhone 上登录 iCloud 确认账户正常
5. 尝试使用扫码登录

### 问题 3：下载失败

**检查步骤**：
1. 检查网络连接
2. 检查存储空间
3. 查看日志文件：
   ```bash
   cat logs/app_*.log | grep -i error
   ```
4. 尝试减少线程数：
   ```yaml
   threads_num: 1
   ```

### 问题 4：下载速度慢

**检查步骤**：
1. 检查网络带宽
2. 增加线程数：
   ```yaml
   threads_num: 5
   ```
3. 关闭其他占用网络的程序
4. 尝试使用有线网络

### 问题 5：文件组织混乱

**检查步骤**：
1. 检查 `folder_format` 配置
2. 确认格式正确：
   ```yaml
   folder_format: "%Y/%Y-%m/%Y-%d"
   ```
3. 重新下载（删除已下载文件）

---

## 技术支持

如果遇到无法解决的问题，请提供以下信息：

1. 操作系统版本
2. Python 版本
3. 错误信息截图
4. 日志文件内容
5. 配置文件内容

---

## 更新日志

### v2.0.0 (2026-07-09)
- ✅ 新增多线程下载
- ✅ 新增断点续传
- ✅ 新增自动重试
- ✅ 新增磁盘空间检查
- ✅ 新增日志系统
- ✅ 优化错误处理
- ✅ 优化用户界面

### v1.0.0 (2026-07-09)
- ✅ 初始版本发布
- ✅ 支持 iCloud 照片下载
- ✅ 支持扫码登录
- ✅ 支持进度显示

---

## 附录

### A. 配置文件完整示例

```yaml
# iCloud 照片迁移助手配置文件

# Apple ID（可选）
apple_id: ""

# 存储路径（必填）
nas_path: "/Volumes/MyNAS/iCloud-Photos"

# Cookie 存储目录
cookie_dir: "./cookies"

# 文件夹格式
folder_format: "%Y/%Y-%m/%Y-%d"

# 下载线程数（1-10）
threads_num: 3

# 仅下载新文件
only_new: true

# 下载照片
download_photos: true

# 下载视频
download_videos: true

# 跳过实况照片
skip_live_photos: false

# 最大重试次数
max_retries: 3

# 日志级别
log_level: "INFO"

# 日志输出到文件
log_to_file: true
```

### B. 命令行参考

```bash
# 启动程序
python3 main.py

# 安装依赖
pip3 install -r requirements.txt

# 查看日志
tail -f logs/app_*.log

# 删除数据库（重新开始）
rm -f migration_state.db

# 删除配置（重新配置）
rm -f config.yaml
```

### C. 文件结构

```
iCloud照片迁移助手/
├── main.py                    # 主程序
├── 启动iCloud迁移助手.command  # 启动脚本
├── config.yaml                # 配置文件
├── config.yaml.example        # 配置示例
├── requirements.txt           # 依赖列表
├── build.py                   # 打包脚本
├── SOP_使用指南.md            # 本文档
├── core/                      # 核心模块
│   ├── config.py              # 配置管理
│   ├── constants.py           # 常量定义
│   ├── logger.py              # 日志系统
│   ├── icloud_worker.py       # 下载模块
│   ├── state_db.py            # 数据库
│   ├── dedup.py               # 去重模块
│   ├── nas_detector.py        # NAS 检测
│   └── qrcode_auth.py         # 二维码认证
├── gui/                       # 界面模块
│   ├── modern_style.py        # UI 样式
│   ├── welcome_page.py        # 欢迎页面
│   ├── download_page.py       # 下载页面
│   ├── report_page.py         # 报告页面
│   └── main_window.py         # 主窗口
└── logs/                      # 日志目录
```

---

**文档版本**: v2.0.0  
**更新日期**: 2026-07-09  
**作者**: iCloud 照片迁移助手团队
