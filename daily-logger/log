#!/bin/bash
# 每日记录工具快捷入口

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PYTHON_SCRIPT="$SCRIPT_DIR/enhanced_logger.py"

# 检查 Python 是否可用
if ! command -v python3 &> /dev/null; then
    echo "❌ 未找到 Python3，请先安装 Python3"
    exit 1
fi

# 运行 Python 脚本
python3 "$PYTHON_SCRIPT" "$@"
